import Blocknote from "@/components/main/Blocknote";

const BlockEditor = () => {
 
  // Renders the editor instance using a React component.
  return <Blocknote></Blocknote>;
}

export default BlockEditor